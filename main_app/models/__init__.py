from .models_reservations import Reservation, ReservedRooms, ArchivedReservation
from .models_rooms import Room, RoomPrice
from .models_others import Page, PageSection, Contact, Gallery, HeroGallery, TermWorkList, Reviews
